# Python
__pycache__/
*.pyc
*.pyo
*.pyd
*.db
*.sqlite3
.env
.venv/
venv/
dist/
build/
*.egg-info/
.pytest_cache/

# Node
node_modules/
dist/
.env.local

# IDE
.vscode/
.idea/

# Phase AUTH — admins.json holds plaintext passwords for the static
# admin roster. Per-deployment, MUST NOT be committed.
# `admins.example.json` is checked in as a template.
backend/admins.json

# Claude Code agent worktrees (created by sub-agents, ephemeral)
.claude/
